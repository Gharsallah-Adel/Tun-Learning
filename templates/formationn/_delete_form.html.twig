<form method="post" action="{{ path('formationn_delete', {'id': formationn.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formationn.id) }}">
    <button class="btn">Delete</button>
</form>
