{% extends 'base.html.twig' %}

{% block title %}PlanFormation{% endblock %}

{% block body %}
    <h1>PlanFormation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan_formation.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ plan_formation.numero }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ plan_formation.date ? plan_formation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Salle</th>
                <td>{{ plan_formation.salle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ plan_formation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plan_formation_index') }}">back to list</a>

    <a href="{{ path('plan_formation_edit', {'id': plan_formation.id}) }}">edit</a>

    {{ include('plan_formation/_delete_form.html.twig') }}
{% endblock %}
